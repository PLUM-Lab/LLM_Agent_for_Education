node_modules/

# Local secrets/config - API keys (NEVER upload!)
api-key.js
.env
*.env
OPENAI_API_KEY.txt
*api*key*.txt
*secret*.txt

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/
._*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
temp/

# Build outputs
/dist/
/build/

# Python cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Extracted images from PDFs (generated by parse_qbank.py)
/images/

# Large PDF files (optional - keep in repo for reference or remove)
# Uncomment the following lines if you don't want to upload PDFs:
# Clinical Guidelines/
# Qbanks and Practice Exams/

# Temporary test files
test_validation.py

# Student profiles (test data, should not be committed)
student_profile_*.json
knowledge_tracking_report_*.txt
analysis_report_*.txt

# User activity log (login/logout) - may contain PII
user_activity_log.json

# Per-user logs (button clicks, tutor conversations, usage) - may contain PII
user_logs/

# Local auth user store (plaintext passwords — never commit)
local_users.json

# Ensure API key is never committed
api-key.js
*.key
*secret*

user_activity_log copy.json
