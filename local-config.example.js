// Copy this file to local-config.js (do NOT commit) and set your key locally
// Option A: set into localStorage (persists in your browser)
// localStorage.setItem('openai_api_key', 'sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx');

// Option B: set at runtime via global function (per page load)
// window.setOpenAIAPIKey && window.setOpenAIAPIKey('sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx');


